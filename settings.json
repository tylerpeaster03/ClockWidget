{
  // Settings
  //==============================

  // Select which monitor the clock is displayed on.
  // Monitor numbers are based on the order detected by Windows, which can be seen in Display Settings. (Type "Display Settings" in the Search Bar)
  // For example,"1" would display the clock on Monitor 1 only, "1,2" would display the clock on Monitor 1 and Monitor 2, and "all" would display the clock on all monitors.
  "monitor": "all",

  //==============================

  // App Memory || DO NOT CHANGE, the app uses this to remember settings that are changed outside of this file.
  //==============================
  "position": "",
  "size": ""
  //==============================
}
